# 心理健康分析指导小程序 — 项目架构全景梳理

> 梳理时间：2026-08-11

---

## 一、项目组成

| 目录 | 技术栈 | 角色 |
|------|--------|------|
| `django04y80q6t/` | Django 2.0 + MySQL 5.5+ | 后端 API 服务 |
| `mp-weixin/` | uni-app (Vue 2) → 微信小程序 | 移动端前端（编译产物） |

---

## 二、后端架构 (`django04y80q6t/`)

### 核心设计理念：表驱动 + 元属性声明式框架

```
django04y80q6t/
├── dj2/                    # Django 项目配置
│   ├── settings.py         # 配置：MySQL、中间件链、CORS、支付宝
│   └── urls.py             # 顶层路由：静态文件 + API 分发
├── main/                   # 唯一 Django App（所有业务逻辑）
│   ├── models.py           # 16 个数据模型
│   ├── model.py            # BaseModel 基类（通用 CRUD 封装）
│   ├── urls.py             # 动态路由注册（扫描 *_v.py 自动生成）
│   ├── *_v.py              # 14 个视图文件（每个表一个）
│   ├── schema_v.py         # 通用操作（文件上传/聚合/统计）
│   ├── scl90.py            # SCL-90 心理测评评分引擎
│   └── config_model.py     # 配置表模型
├── xmiddleware/            # 自定义中间件
│   ├── xauth.py            # JWT Token 认证 + 黑名单检查
│   ├── xparam.py           # 请求参数统一解析
│   ├── ratelimit.py        # IP 滑动窗口限流（30次/分钟）
│   └── hive_middleware.py  # Hive 数据同步
├── util/                   # 工具模块
│   ├── auth.py             # 认证/token 签发
│   ├── security.py         # 密码哈希、文件校验、路径安全
│   └── baidubce_api.py     # 百度 AI 人脸识别
├── templates/front/        # uni-app 前端源码（Vue 2 + Vite）
└── scripts/                # 数据库初始化脚本
```

### 16 个数据模型

| 模型 | 中文名 | 关键特性 |
|------|--------|----------|
| `yonghu` | 普通用户 | 可登录(`__authPeople__='是'`)，前台注册 |
| `xinliyisheng` | 心理医生 | 可登录，前台展示(`__foreEndList__='是'`) |
| `users` | 系统管理员 | 可登录，管理员(`__isAdmin__='是'`) |
| `xinqingriji` | 心情日记 | 前台需登录，含敏感词预警，支持点赞 |
| `yuyuezixun` | 预约咨询 | 需审核(`__sfsh__='是'`)，用户/医生权限隔离 |
| `yuyueshiduan` | 预约时段 | 下拉选项 |
| `jiankangyujing` | 健康预警 | SCL-90 测评联动生成 |
| `xinlizhishi` | 心理知识 | 智能推荐 + 点击计数 + 点赞 |
| `zhishifenlei` | 知识分类 | 下拉选项 |
| `news` | 发布信息 | 公告/资讯 |
| `exampaper` | 心理测试试卷 | 含 SCL-90 |
| `examquestion` | 试题 | 5 种题型（单选/多选/判断/填空/问答） |
| `examrecord` | 考试记录 | 答题/评分/SCL-90 计算，用户隔离 |
| `popupremind` | 弹窗提醒 | 定时推送，用户隔离 |
| `storeup` | 收藏表 | 通用收藏/点赞，用户隔离 |
| `discussxinqingriji` | 心情日记评论 | 评论/回复 |
| `discussxinlizhishi` | 心理知识评论 | 评论/回复 |

### 元属性系统（框架精华）

| 元属性 | 含义 |
|--------|------|
| `__authPeople__` | 是否为可登录用户表 |
| `__loginUserColumn__` | 登录用户名字段名 |
| `__sfsh__` | 是否需要审核 |
| `__authSeparate__` | 后台列表是否按用户隔离 |
| `__thumbsUp__` | 是否支持点赞/踩 |
| `__intelRecom__` | 是否支持智能推荐 |
| `__browseClick__` | 是否支持点击计数 |
| `__foreEndListAuth__` | 前台列表是否按用户权限隔离 |
| `__foreEndList__` | 前台列表可见性('否'/'是'/'前要登') |
| `__isAdmin__` | 是否为管理员表 |

### 中间件链（按顺序执行）

1. `RateLimitMiddleware` — 频控限流（普通 30次/分钟，登录 10次/分钟）
2. `Xparam` — 请求参数统一解析（GET/POST/JSON Body）
3. `Xauth` — JWT Token 认证 + 白名单路径 + 黑名单检查
4. `CorsMiddleware` — CORS 跨域

### SCL-90 测评系统

- 90 题量表，5 级评分制
- 10 个因子：躯体化、强迫症状、人际关系敏感、抑郁、焦虑、敌对、恐怖、偏执、精神病性、附加因子
- 阳性筛查标准：总分 ≥ 160 或 阳性项目数 > 43 或 任一因子 ≥ 2
- 测评完成后自动生成健康预警记录

---

## 三、前端架构 (`mp-weixin/`)

### 技术栈

- **框架**：uni-app (Vue 2) + HBuilderX 编译
- **UI 库**：ColorUI
- **状态管理**：无 Vuex，使用 `uni.storage` 本地存储
- **API 通信**：`uni.request` + JWT Token 注入
- **页面数**：57 个页面

### 全局注入体系

| 属性 | 来源 | 用途 |
|------|------|------|
| `$utils` | `utils/utils.js` | 通用工具（跳转、提示、日期等） |
| `$api` | `api/index.js` | 所有 API 请求方法 |
| `$base` | `api/base.js` | 后端 API 基础 URL |
| `$config` | `utils/config.js` | 收藏/点赞配置表 |
| `$validate` | `utils/validate.js` | 表单校验 |
| `isAuth` / `isAuthFront` | `utils/system.js` | 权限判断 |

### 页面导航

```
TabBar: 首页 ↔ 心情日记 ↔ 我的

首页 → 轮播图 / 心理知识推荐 / 发布信息 / 菜单导航
心情日记 → 写日记 / 日记详情 / 评论
我的 → 用户信息 / 后台管理菜单 / 修改密码
     → 心情日记管理 / 预约咨询 / 健康预警 / 测评 / 收藏

测评流程：试卷列表 → 答题页 → 提交 → 测评报告（含 SCL-90 因子分析）
```

### 认证流程

```
App 启动 → 首页
需要登录 → 登录页（选择角色：用户/心理医生）→ 成功后跳转
注册 → 注册页（用户/心理医生）→ 注册成功 → 返回登录
忘记密码 → 重置密码页
```

### 弹窗提醒机制

- App.vue 每 20 秒轮询 `popupremind` 表
- 检查提醒类型：全局提醒 / 个人提醒
- 时间到达且未弹窗过 → `uni.showModal` 弹出
- 在 `storeup` 表记录(type=61)，防止重复弹窗

---

## 四、数据流

```
微信小程序 (uni-app)
    │
    │ uni.request + JWT Token (header)
    ▼
Django 后端 (dj2)
    │
    ├── RateLimitMiddleware (频控限流)
    ├── Xparam (参数解析)
    ├── Xauth (JWT 认证 + 黑名单)
    │
    ▼
main/urls.py (动态路由分发)
    │
    ▼
*_v.py 视图 (CRUD 逻辑)
    │
    ▼
BaseModel (DB 操作抽象)
    │
    ▼
MySQL 8.0 (xinli 数据库)
```

---

## 五、比赛评审视角的关键亮点

1. **SCL-90 专业测评**：完整的 90 题量表 + 10 因子分析 + 自动预警，是核心差异化功能
2. **心情日记风险预警**：日记内容敏感词检测（自杀/焦虑等关键词），联动健康预警
3. **表驱动框架**：新增业务表只需创建 `_v.py` 文件，路由自动注册
4. **元属性声明式设计**：通过类属性控制审核/权限/点赞/推荐等行为，框架层统一处理
5. **三层权限体系**：用户/医生/管理员，前后台权限隔离，数据安全
6. **弹窗提醒**：定时推送心理健康提醒，主动关怀用户