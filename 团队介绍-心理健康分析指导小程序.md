# 心理健康分析指导小程序 — 团队介绍与项目全貌

> 生成时间：2026年8月11日
> 用途：团队新成员了解项目全貌

---

## 一、项目概述

本项目是一个**心理健康分析指导微信小程序**，面向普通用户提供心理健康自评、心情记录、心理知识学习、在线预约咨询等服务。系统由 Django 后端 + 微信小程序前端组成，已具备完整的上线能力。

核心定位：**以 SCL-90 专业心理量表为核心，结合心情日记、智能预警、在线咨询，构建一个"测评—记录—预警—干预"的闭环心理健康服务平台。**

---

## 二、项目组成

项目由两个代码仓库组成，位于 `D:\project\xinliguanli\` 目录下：

| 目录 | 技术栈 | 角色 |
|------|--------|------|
| `django04y80q6t/` | Django 2.0 + MySQL 8.0 + Redis | 后端 API 服务 + 管理后台 |
| `mp-weixin/` | uni-app（Vue 2）编译产物 | 微信小程序前端 |

前端源代码位于 `django04y80q6t/templates/front/`，是一个 uni-app + Vue 2 项目，通过 HBuilderX 编译生成 `mp-weixin/` 目录下的微信小程序代码。

---

## 三、后端架构详解

### 3.1 技术栈

- 框架：Django 2.0
- 数据库：MySQL 8.0（数据库名：`xinli`，字符集：utf8mb4）
- 缓存：Redis（用于 Session 管理）
- 认证：JWT Token（自研实现，非 Django REST Framework）
- 邮件：QQ邮箱 SMTP（SSL，端口465）
- 支付：支付宝（已配置 RSA2 密钥）
- 第三方服务：百度 AI 人脸识别、Hive 数据仓库（可选）

### 3.2 目录结构

```
django04y80q6t/
├── dj2/                    # Django 项目配置
│   ├── settings.py         # 全局配置（数据库、中间件、CORS、支付宝等）
│   └── urls.py             # 顶层路由分发
├── main/                   # 唯一 Django App（所有业务逻辑集中于此）
│   ├── models.py           # 数据模型定义（18个模型类）
│   ├── model.py            # BaseModel 基类（通用 CRUD 封装）
│   ├── urls.py             # 动态路由注册（扫描 *_v.py 自动生成路由）
│   ├── *_v.py              # 各数据表的视图文件（每个表一个文件）
│   ├── schema_v.py         # 通用操作（文件上传、统计、聚合等）
│   ├── scl90.py            # SCL-90 心理量表评分引擎
│   └── config_model.py     # 配置表模型
├── xmiddleware/            # 自定义中间件
│   ├── xauth.py            # JWT Token 认证 + 黑名单
│   ├── xparam.py           # 请求参数统一解析
│   ├── ratelimit.py        # IP 滑动窗口限流
│   └── hive_middleware.py  # Hive 数据同步
├── util/                   # 工具模块
│   ├── auth.py             # 认证与 Token 签发
│   ├── security.py         # 密码哈希、文件校验、安全工具
│   └── baidubce_api.py     # 百度 AI 人脸识别接口
├── templates/front/        # uni-app 前端源码（Vue 2 + Vite）
├── scripts/                # 数据初始化脚本
├── db/                     # 数据库初始化 SQL
├── media/                  # 用户上传文件存储
├── config.ini              # 数据库与 Redis 连接配置
├── requirements.txt        # Python 依赖清单
├── 安装.bat                # 一键安装脚本
└── 运行.bat                # 一键启动脚本
```

### 3.3 核心设计理念：表驱动 + 元属性声明式框架

这是本项目后端的最大技术亮点。开发者**不需要手写增删改查接口**，只需：

1. 在 `models.py` 中定义一个 Django Model 类，并通过类属性声明行为（如是否需登录、是否需审核、是否支持点赞等）
2. 创建一个对应的 `表名_v.py` 视图文件

系统会自动为每个数据表生成标准的 RESTful API 接口，包括：分页查询、条件查询、添加、修改、删除、详情、点赞、批量操作、Excel 导入导出等。

路由也是自动注册的——`main/urls.py` 会扫描目录下所有 `*_v.py` 文件，自动为每个表生成标准路由，URL 格式为：`/{表名}/{操作}`，例如 `/xinli/yonghu/login`、`/xinli/xinqingriji/page`。

### 3.4 元属性系统

每个模型类通过类属性声明来控制行为，这是一种声明式设计：

| 元属性 | 含义 |
|--------|------|
| `__authPeople__` | 是否为可登录用户表（'是'/'否'） |
| `__loginUserColumn__` | 登录用户名字段名 |
| `__sfsh__` | 是否需要审核（'是'/'否'） |
| `__authSeparate__` | 后台列表是否按用户隔离（'是'/'否'） |
| `__thumbsUp__` | 是否支持点赞/踩（'是'/'否'） |
| `__intelRecom__` | 是否支持智能推荐（'是'/'否'） |
| `__browseClick__` | 是否支持点击计数（'是'/'否'） |
| `__foreEndList__` | 前台列表可见性（'否'/'是'/'前要登'） |
| `__foreEndListAuth__` | 前台列表是否按用户权限隔离 |
| `__isAdmin__` | 是否为管理员表（'是'/'否'） |

例如，心情日记模型通过 `__thumbsUp__='是'` 声明支持点赞，`__foreEndList__='前要登'` 声明前台列表需要登录才能访问。框架层会统一处理这些声明，在视图中自动注入对应的逻辑。

### 3.5 数据模型（18个）

| 模型 | 数据库表名 | 中文名 | 关键特性 |
|------|-----------|--------|----------|
| `yonghu` | `yonghu` | 普通用户 | 可登录、可前台注册，字段：账号、密码、姓名、性别、头像、手机 |
| `xinliyisheng` | `xinliyisheng` | 心理医生 | 可登录、前台展示，字段：工号、密码、姓名、性别、头像、电话、简介 |
| `users` | `users` | 系统管理员 | 可登录、管理员权限，字段：账号、密码、姓名、角色 |
| `xinqingriji` | `xinqingriji` | 心情日记 | 前台需登录，支持点赞，含敏感词预警 |
| `yuyuezixun` | `yuyuezixun` | 预约咨询 | 需审核，关联用户和医生，用户/医生权限隔离 |
| `yuyueshiduan` | `yuyueshiduan` | 预约时段 | 下拉选项字典表 |
| `jiankangyujing` | `jiankangyujing` | 健康预警 | SCL-90 测评联动自动生成 |
| `xinlizhishi` | `xinlizhishi` | 心理知识 | 智能推荐 + 点击计数 + 点赞，关联医生表 |
| `zhishifenlei` | `zhishifenlei` | 知识分类 | 下拉选项字典表 |
| `news` | `news` | 发布信息 | 公告/资讯，前台公开 |
| `exampaper` | `exampaper` | 心理测试试卷 | 试卷名称、时长、状态、限考次数 |
| `examquestion` | `examquestion` | 试题 | 5 种题型：单选/多选/判断/填空/问答 |
| `examrecord` | `examrecord` | 考试记录 | 答题记录、评分、SCL-90 计算，用户隔离 |
| `popupremind` | `popupremind` | 弹窗提醒 | 定时推送，用户隔离 |
| `storeup` | `storeup` | 收藏表 | 通用收藏/点赞，用户隔离 |
| `discussxinqingriji` | `discussxinqingriji` | 心情日记评论 | 评论/回复，支持赞/踩/置顶 |
| `discussxinlizhishi` | `discussxinlizhishi` | 心理知识评论 | 评论/回复，支持赞/踩/置顶 |
| `TokenBlacklist` | `token_blacklist` | Token黑名单 | 记录已注销的 Token |

### 3.6 中间件链（按顺序执行）

请求进入 Django 后，依次经过以下中间件处理：

1. **RateLimitMiddleware**（频控限流）：普通接口 30 次/分钟/IP，登录/注册接口 10 次/分钟/IP
2. **Xparam**（参数解析）：统一解析 GET 参数、POST JSON、POST FormData，存入 `request.session["req_dict"]`
3. **Xauth**（Token 认证）：JWT Token 验证，包含白名单路径（登录/注册/首页等免认证），支持 Token 黑名单
4. **CorsMiddleware**（跨域）：允许前端跨域访问

### 3.7 SCL-90 心理测评系统（核心差异化功能）

这是项目的核心功能模块，位于 `main/scl90.py`：

- 90 道题，5 级评分制（1=没有，2=很轻，3=中等，4=偏重，5=严重）
- 10 个因子维度：躯体化、强迫症状、人际关系敏感、抑郁、焦虑、敌对、恐怖、偏执、精神病性、附加因子
- 每个因子都配有专业指导建议
- 阳性筛查标准：总分 ≥ 160 或 阳性项目数 > 43 或 任一因子分 ≥ 2
- 测评完成后自动生成健康预警记录，关联到用户

### 3.8 部署与运行

一键安装（`安装.bat`）：
1. 创建/激活 Python 3.8 虚拟环境
2. 安装 Python 依赖（使用阿里云镜像加速）
3. 初始化数据库（建表 + 导入初始数据）
4. 执行 Django 迁移
5. 导入 SCL-90 量表数据
6. 创建超级管理员账号（abo/abo）

一键运行（`运行.bat`）：
- 启动 Django 开发服务器：`python manage.py runserver --insecure 0.0.0.0:8080`
- 管理后台：`http://localhost:8080/admin/dist/index.html`
- 用户端 H5：`http://localhost:8080/front/index.html`

---

## 四、前端架构详解

### 4.1 技术栈

- 框架：uni-app（Vue 2），通过 HBuilderX 编译为微信小程序
- UI 库：ColorUI
- 状态管理：无 Vuex，使用 `uni.storage` 本地存储
- API 通信：`uni.request` + JWT Token 注入
- 页面数：57 个页面（23 个业务模块）
- 小程序 AppID：`wxbd147a4a183e0468`

### 4.2 全局注入体系

前端通过 Vue 原型链挂载以下全局属性，所有页面和组件均可直接使用：

| 属性 | 来源 | 用途 |
|------|------|------|
| `$utils` | `utils/utils.js` | 通用工具：页面跳转、消息提示、日期格式化、城市列表等 |
| `$api` | `api/index.js` | 所有 API 请求方法的集合 |
| `$base` | `api/base.js` | 后端 API 基础 URL（默认 `http://localhost:8080/django04y80q6t/`） |
| `$config` | `utils/config.js` | 收藏/点赞配置表 |
| `$validate` | `utils/validate.js` | 表单校验：邮箱、手机、身份证、URL 等 |
| `isAuth` | `utils/system.js` | 后台权限判断 |
| `isAuthFront` | `utils/system.js` | 前台权限判断 |
| `encryptDes`/`decryptDes` | `utils/des.js` | DES 加密/解密 |
| `encryptAes`/`decryptAes` | `utils/des.js` | AES 加密/解密 |

### 4.3 页面导航结构

```
TabBar（底部导航）：首页 ↔ 心情日记 ↔ 我的

首页
├── 轮播图 Banner
├── 心理知识推荐（智能推荐算法）
├── 发布信息/公告
└── 菜单导航
    ├── 心理医生列表
    ├── 心理知识列表
    ├── 心理测评入口
    └── 发布信息

心情日记
├── 日记列表（瀑布流）
├── 写日记（含敏感词检测）
├── 日记详情
└── 评论/回复

我的（个人中心）
├── 用户信息（头像、昵称等）
├── 后台管理菜单（根据角色不同）
│   ├── 心情日记管理
│   ├── 预约咨询管理
│   ├── 健康预警管理
│   ├── 心理测试试题管理
│   ├── 测评报告管理
│   └── 收藏管理
├── 修改密码
└── 退出登录

测评流程
试卷列表 → 答题页（90题，5级评分） → 提交 → 测评报告
                                           ├── 总分
                                           ├── 阳性项目数
                                           ├── 10个因子雷达图
                                           └── 各因子指导建议
```

### 4.4 认证流程

```
App 启动
  ↓
首页（无需登录可浏览）
  ↓
需要登录的操作 → 跳转登录页
  ├── 选择角色：普通用户 / 心理医生
  ├── 输入账号密码 → 登录成功 → 返回
  └── 没有账号 → 注册页（用户注册/医生注册）
  ↓
忘记密码 → 重置密码页
```

### 4.5 弹窗提醒机制

- App.vue 每 20 秒轮询后端的 `popupremind` 表
- 检查提醒类型：全局提醒（所有用户可见）或 个人提醒（指定用户可见）
- 时间到达且未弹窗过 → 弹出 `uni.showModal`
- 在 `storeup` 表记录（type=61），防止重复弹窗

### 4.6 权限体系

前端通过 `utils/menu.js` 定义了完整的三层角色权限：

**普通用户（yonghu）：**
- 前台：浏览心理医生列表、心理知识列表、发布信息
- 后台：管理自己的心情日记、预约咨询、健康预警、测评记录、收藏
- 支持前台登录和注册

**心理医生（xinliyisheng）：**
- 后台：管理心情日记、预约咨询、健康预警、心理知识、知识分类、发布信息、弹窗提醒、收藏、评论
- 支持登录，不支持前台注册
- 预约咨询需要审核通过才能生效

**系统管理员（users）：**
- 拥有全部权限，可访问 Django Admin 后台

---

## 五、数据流全景

```
微信小程序（uni-app）
    │
    │ uni.request + JWT Token（Header 中携带）
    │ Base URL: http://localhost:8080/django04y80q6t/
    │
    ▼
Django 后端（dj2）
    │
    ├── RateLimitMiddleware     ← 频控限流（30次/分钟/IP）
    ├── Xparam                   ← 参数统一解析
    ├── Xauth                    ← JWT 认证 + 白名单 + 黑名单
    │
    ▼
main/urls.py（动态路由分发）
    │
    ▼
*_v.py 视图（CRUD 业务逻辑）
    │
    ▼
BaseModel 基类（数据库操作抽象）
    │
    ▼
MySQL 8.0 数据库（xinli 库）
    │
    └── Redis（Session 缓存）
```

---

## 六、开发环境搭建指南

### 6.1 环境要求

- Python 3.8+（推荐 3.8，项目有专属 venv38 虚拟环境）
- MySQL 8.0（数据库名 `xinli`，字符集 utf8mb4）
- Redis（用于缓存 Session）
- HBuilderX（用于编译 uni-app 前端）
- 微信开发者工具（用于预览/调试小程序）
- Node.js（可选，用于前端依赖管理）

### 6.2 后端启动步骤

1. 确保 MySQL 和 Redis 已启动，并在 `config.ini` 中配置正确的连接信息
2. 运行 `安装.bat` 完成一键安装（首次）
3. 运行 `运行.bat` 启动 Django 开发服务器（端口 8080）
4. 访问 `http://localhost:8080/admin/dist/index.html` 进入管理后台

### 6.3 前端启动步骤

1. 用 HBuilderX 打开 `django04y80q6t/templates/front/` 目录
2. 修改 `api/base.js` 中的 `baseUrl` 为后端实际地址
3. 在 HBuilderX 中点击"运行 → 运行到小程序模拟器 → 微信开发者工具"
4. 微信开发者工具自动打开，即可预览和调试

### 6.4 数据库配置

`config.ini` 文件内容示例：
```ini
[mysql]
type = mysql
host = 127.0.0.1
port = 3306
user = root
passwd = 123456
db = xinli
charset = utf8mb4

[redis]
host = 127.0.0.1
port = 6379
passwd = 123456
```

---

## 七、技术架构总结与亮点

### 7.1 架构特点

1. **表驱动 + 元属性声明式框架**：后端只需定义模型类并声明元属性，系统自动生成全套 RESTful API，新增业务表几乎零代码。这是本项目最大的技术特色。

2. **单 App 集中式架构**：所有业务逻辑集中在 `main` 这一个 Django App 中，通过视图文件（`*_v.py`）和路由自动扫描实现模块化，避免了多 App 带来的配置碎片化。

3. **自研中间件体系**：不依赖 Django REST Framework，自研了参数解析、JWT 认证、频率限制三件套中间件，代码完全可控。

4. **三层权限体系**：普通用户、心理医生、系统管理员，前后台权限精细隔离，数据安全有保障。

### 7.2 核心差异化功能

1. **SCL-90 专业心理测评**：完整的 90 题量表、10 因子分析、自动评分、阳性筛查、指导建议生成，是项目的核心差异化功能。

2. **心情日记 + 敏感词预警**：用户在日记中提及自杀、焦虑等关键词，系统自动联动健康预警，实现主动危机干预。

3. **智能推荐**：心理知识模块支持智能推荐算法，根据用户行为推荐相关内容。

4. **弹窗提醒**：定时推送心理健康提醒，主动关怀用户，支持全局提醒和个人提醒。

5. **在线预约咨询**：用户可预约心理医生，需要审核通过，支持完整的咨询流程管理。

### 7.3 技术债务与改进方向

以下是一些已知可改进的地方，供团队后续参考：

1. **Django 版本升级**：当前使用 Django 2.0（已停止维护），建议升级到 Django 4.2 LTS 或更高版本。
2. **前端框架升级**：当前 uni-app + Vue 2，可考虑升级到 Vue 3 + uni-app 3.x。
3. **状态管理**：当前前端无 Vuex/Pinia，使用 `uni.storage` 本地存储，复杂场景下状态管理较为分散。
4. **限流方案**：当前限流使用内存字典存储（单进程有效），生产环境需改为 Redis 实现。
5. **API 文档**：缺少 API 文档（如 Swagger），建议后续补充。
6. **单元测试**：当前测试覆盖不足，核心业务逻辑（如 SCL-90 评分）建议补充测试用例。
7. **数据库迁移管理**：当前使用 `migrate --fake-initial`，建议规范化 Django migration 管理流程。

---

## 八、文件清单速查

| 文件/目录 | 说明 |
|-----------|------|
| `django04y80q6t/dj2/settings.py` | Django 全局配置 |
| `django04y80q6t/main/models.py` | 所有数据模型定义 |
| `django04y80q6t/main/model.py` | BaseModel 基类（通用 CRUD） |
| `django04y80q6t/main/urls.py` | 动态路由注册 |
| `django04y80q6t/main/scl90.py` | SCL-90 评分引擎 |
| `django04y80q6t/xmiddleware/xauth.py` | JWT 认证中间件 |
| `django04y80q6t/xmiddleware/xparam.py` | 参数解析中间件 |
| `django04y80q6t/xmiddleware/ratelimit.py` | 频率限制中间件 |
| `django04y80q6t/config.ini` | 数据库/Redis 连接配置 |
| `django04y80q6t/requirements.txt` | Python 依赖清单 |
| `django04y80q6t/templates/front/` | uni-app 前端源码 |
| `django04y80q6t/templates/front/utils/menu.js` | 角色权限配置 |
| `django04y80q6t/templates/front/api/base.js` | API 基础 URL 配置 |
| `django04y80q6t/templates/front/api/index.js` | 所有 API 方法集合 |
| `django04y80q6t/templates/front/api/http.js` | HTTP 请求封装 |
| `mp-weixin/app.json` | 小程序全局配置（页面路由、TabBar） |
| `mp-weixin/project.config.json` | 小程序项目配置（AppID 等） |
| `项目架构梳理.md` | 之前梳理的架构文档（可参考） |

---

## 九、团队协作建议

1. **后端开发**：主要在 `main/` 目录下工作，新增业务表需在 `models.py` 中定义模型 + 创建对应 `_v.py` 视图文件，路由会自动注册。
2. **前端开发**：在 `django04y80q6t/templates/front/` 下开发 Vue 源码，通过 HBuilderX 编译到微信小程序。不要在 `mp-weixin/` 目录下直接修改（那是编译产物）。
3. **数据库变更**：修改模型后需要执行 `python manage.py makemigrations` 和 `python manage.py migrate`。
4. **代码规范**：后端使用 Python 3，前端使用 ES6+，建议配置 ESLint 和 Prettier 统一代码风格。
5. **Git 工作流**：建议使用 feature 分支开发，合并到 main 分支前进行 code review。

---

_本文档会随着项目演进持续更新。如有疑问，可参考 `项目架构梳理.md` 获取更多技术细节。_